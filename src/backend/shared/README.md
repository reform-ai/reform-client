# Shared Backend Components

This directory contains backend components that are shared across all exercises.

## Components

### live-video/
Receives and processes streamed frames from the frontend camera.

### pose-estimation/
Pose estimation component that identifies body part locations. This is shared across all exercises as the pose detection logic is the same regardless of exercise type.

